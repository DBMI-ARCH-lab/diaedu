<div class="block breadcrumb">
  <h3>
    <i class="fa fa-random"></i>
    {{i18n diaedu.how_got_here}}
  </h3>
  {{#unless loading}}
    <ol>
      {{#each breadcrumb.crumbs}}
        <li>
          <div>{{dataType.singularTitle}}</div>
          {{#linkTo 'kb_obj.show_with_breadcrumb' dataType this}}<strong>{{name}}</strong>{{/linkTo}}
        </li>
      {{/each}}
    </ol>
  {{/unless}}
</div>